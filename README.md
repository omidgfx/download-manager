# Fletch Downloader

A smart multipart download manager with scheduling, real-time progress, and a clean UI.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Configure the `.env` file with your settings (see `.env.example`).
3. Run the app:
   `npm run dev`